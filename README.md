git remote add origin https://github.com/NgTai-Beg/dacs.git
git add .
git commit -m "Initial commit"  # Hoặc thông điệp tùy chọn của bạn
git push -u origin master
git pull origin master  # Nếu bạn sử dụng nhánh master
